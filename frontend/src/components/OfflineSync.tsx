import React from "react";

const OfflineSync: React.FC = () => {
  // TODO: Add local caching and sync logic
  return (
    <div className="p-4 border rounded-lg bg-white/80 shadow">
      <h3 className="font-bold mb-2">Offline Support</h3>
      <div>Local caching & sync when reconnected.</div>
      {/* TODO: Add offline status and sync UI */}
    </div>
  );
};

export default OfflineSync;
